// Example JavaScript to dynamically update content
document.addEventListener('DOMContentLoaded', () => {
    const mainHeader = document.getElementById('main-header');
    // Add any dynamic behaviors or content updates here
});
